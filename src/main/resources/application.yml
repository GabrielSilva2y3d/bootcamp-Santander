server:
  port: 8080
  servlet:
    context-path: /bootcamp

spring:
  datasource:
    url: jdbc:mysql://localhost/bootcamp_santander
    username: root
    password:

  jpa:
    show-sql: true
    hibernate: 
      ddl-auto: create-drop
